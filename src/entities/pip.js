// Definer en klasse for at kunne oprette objekter
export class Pip {
  constructor(content, created, username, image) {
    this.content = content;
    this.created = created;
    this.username = username;
    this.image = image;
  }
}
